<!DOCTYPE html>
<html>
  <head>
    <title>ShowdownDj</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header class="bg-indigo-600">
    <nav class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8" aria-label="Top">
      <div class="flex w-full items-center justify-between border-b border-indigo-500 py-6 lg:border-none">
        <% if @spotify_user %>
          <% if device = @spotify_user.devices.select {|d| d.is_active }.first&.name %>
            <p class="text-white">Playing From: <strong><%= device %></strong></p>
          <% else %>
            <p class="text-white">No Spotify apps are playing on any devices.</p>
          <% end %>
        <% else %>
          <%= link_to 'Authenticate with Spotify', '/auth/spotify', class: "inline-block rounded-md border border-transparent bg-white py-2 px-4 text-base font-medium text-indigo-600 hover:bg-indigo-50" %>
        <% end %>
        <%= link_to '⟳', root_path, class: "inline-block rounded-md border border-transparent bg-white py-2 px-4 text-base font-medium text-indigo-600 hover:bg-indigo-50" %>
      </div>
    </nav>
  </header>
  <%= yield %>
  </body>
</html>
